{
  "name": "git-grep",
  "main": "./lib/git-grep",
  "version": "0.3.0",
  "description": "`git grep` in atom editor",
  "activationEvents": [
    "git-grep:grep"
  ],
  "repository": "git://github.com/mizchi/atom-git-grep",
  "license": "MIT",
  "engines": {
    "atom": ">0.50.0"
  },
  "dependencies": {}
}
